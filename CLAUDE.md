# CLAUDE.md — 黑客松·alpaca

**进来先读 [黑客松-alpaca.seed.md](黑客松-alpaca.seed.md)。** 只读种子就能开工。判据的落点表在图书馆 `收件/长推理猎场/`，种子已经把它收成 y 和验收。

## 进来第一件事

```bash
.venv/bin/python 跑.py 体检
```

不绿就停，报原文。⛔ 不许猜着改密钥。

⛔ 密钥只在 `.env`。⛔ `ALPACA_PAPER_TRADE` 默认 true，本场不许改 false。
⛔ 账户号、order id、跑到第几笔 —— 这些是事实类内容，**⛔ 不在本门规抄值**，去 `账/` 和 `日志/` 查。

## 这里有什么

| 文件 | 管什么 |
|---|---|
| `闸.py` | 七条规则的下单闸。**纯函数、不联网、不抛异常**，任何算不出来的东西一律拦（fail-closed）。`闸自检.py` 十个假用例。 |
| `手.py` | **下单路径上的那只手，全程走 MCP**（`uvx alpaca-mcp-server`，stdio）。⛔ 不许绕过它直连 HTTP——绕过去落点闸 3 就没了。 |
| `账.py` | 只增不改的 JSONL 账本，一天一个文件，北京时间。`渲染中文()` 出给人看的当日流水。 |
| `判.py` | 挑腿 + 出**落纸判断书**。纯函数。 |
| `跑.py` | 中文命令行：`体检 / 看 / 演闸 / 开 / 平 / 复盘`。 |
| `账/`、`日志/` | 运行时账，⛔ 不进 git。 |

## ⛔ 三条不许动的顺序

1. **`开` 的顺序钉死：先落纸判断 → 再过闸 → 才下单。**验收 3「来路先于结果」靠这个顺序保证，⛔ 不靠自觉。
2. **闸拦下不是错误**，退出码 0。拦的理由要进账本。
3. **问 4（第一屏给不懂的人看 15 秒）园主跑，⛔ AI 不许代。**

## 撞出来的（撞一次补一条）

1. **MCP 的返回套着两层信封**：外层 `{"_alpaca_mcp_security":…, "data":…}`，取列表的工具在里面还有一层 `{"result": […]}`。
   ⛔ 不拆的话 `持仓()`／`订单()` **静默返回空表**——有仓位也读成 0，闸的「未平仓组数」当场被骗，**⛔ 而且不报错**。
   ⚠️ 同一个形状踩了两次（第二次是 `result`）。`手._拆信封` 两层都拆了。
2. **`get_option_chain` 的 `limit` 封顶 1000**，而 SPY 一周七个到期日有 2534 个合约，按合约代码排序 ⟹ 只取第一页会把要用的行权价整段切掉，返回看起来还是「正常的一大批」。
   ⟹ `手.期权链` 用 `next_page_token` 翻完；调用方另外用 `行权价下限/上限` 收窄。
3. **服务端启动横幅走 stderr**，会糊满录屏画面。已收进 `日志/mcp服务端.log`。
4. **休市也能下单**：2026-08-30（周日）下过一张多腿单，`status=accepted`，拿到 order id。
   ⟹ 「等开盘」等的是**成交**，⛔ 不是「报文收不收」。这条在开盘前就该验，⛔ 别留到 21:30 才发现载荷写错。
5. **上游依赖没钉，会半夜换刀**：`uvx alpaca-mcp-server` 不钉版本，服务端对 fastmcp 的约束 `>=3.1.0` 无上界。2026-09-01 02:20 fastmcp 4.0.0 上架，02:49 那轮定时跑 uvx 重解析装上它——4.0.0 拆了 `fastmcp.tools.tool`，服务端 import 即崩，客户端只看到 ExceptionGroup 外层。清缓存重试没用（重解析还是拿到它），这不是缓存过期，是约束缺口。
   ⟹ `手.py` 起服务端带 `--with fastmcp<4` 钉死，`mcp.json.example` 同步；启动失败的摘要递归摘开子异常，真因直接进报错，⛔ 不再只靠 stderr 日志翻。
